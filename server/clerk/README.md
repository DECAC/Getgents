# Auth — Clerk EU

**Non branché dans ce commit.**

Ce dossier est un placeholder pour l'intégration Clerk (auth EU). La structure est prête mais aucun provider réel n'est connecté.

- Provider Clerk à intégrer dans `app/layout.tsx` quand branché
- Toute la logique de session utilisateur reste mockée côté client
- Région : EU (exigence RGPD actée par le board)
